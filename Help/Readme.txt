To run game, copy contents of 'Executables' to root directory
and run MainMenu.exe from that folder.

Additional Features:
SpriteSquash:A sprite editing utility.

Note:All filenames must end with an additional .txt as they are text files.